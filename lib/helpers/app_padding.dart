import 'package:flutter/cupertino.dart';

class AppPadding {
  static const mainPading = EdgeInsets.all(15);
  static const mainSidePading = EdgeInsets.only(left: 15, right: 15);
  static const largeSidePading = EdgeInsets.only(left: 25, right: 25);
}
